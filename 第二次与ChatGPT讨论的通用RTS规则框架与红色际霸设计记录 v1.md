# 第二次与ChatGPT讨论的通用RTS规则框架与红色际霸设计记录 v1

> 记录日期：2026-08-25  
> 项目仓库：`ShrinkShi/RA2YR-bycpp`  
> 文档性质：第二次正式架构讨论记录；用于冻结“RA2YR 兼容引擎 → 通用 RTS 引擎”方向下已经达成一致的玩法规则、抽象边界与后续专题  
> 状态：**阶段性设计基线。本文只记录当前已经明确确认的决定；未确认的问题会明确标记为“待讨论”，不得因为出现在本文中就视为最终实现要求。**

---

## 0. 与第一次讨论文档的关系

第一次完整讨论记录见：

- [`第一次与ChatGPT讨论的RA2YR C++ Engine 总实施计划 v1.md`](./第一次与ChatGPT讨论的RA2YR%20C%2B%2B%20Engine%20总实施计划%20v1.md)

第一次讨论解决的核心问题是：

1. 从 Unity 阶段重新出发，改用 C++23 构建 code-first 的专用 RTS Runtime；
2. 以 Stock Yuri's Revenge 1.001、传统 RA2YR MOD、MIX/INI/MAP/SHP/VXL/HVA/TMP/PAL 等原始数据兼容作为第一阶段硬目标；
3. 建立 Classic / Compatibility 路线，避免“测试存在就等于客户端完成”的错误验收方式。

第二次讨论并不推翻上述目标，而是进一步扩大长期边界：

> **RA2YR-bycpp 不再只被视为“重新实现 `gamemd.exe` 的专用兼容运行时”，而是以 RA2YR 兼容作为第一个、也是最严格的兼容规则集，逐步演进为一个数据驱动的通用 RTS 引擎。**

“红色际霸”将成为第一个重要的混合规则压力测试：在保留 RA2YR 人类阵营、资源、美术和兼容能力的同时，引入类似《星际争霸 II》虫族、神族的生产、技能、移动、护盾、科技等玩法，并允许这些机制在同一局游戏中交叉存在。

因此新的长期原则是：

> **Classic 兼容目标不取消；通用化不能以破坏传统 RA2YR 内容兼容为代价。**

同时：

> **不同阵营的玩法可以完全不同，但底层应尽量通过统一的接口、策略、组件和数据定义表达，而不是为每个阵营写死特殊分支。**

---

# 第一部分：项目长期定位

## 1. 通用 RTS 引擎，而不是“RA2 + SC2 特判集合”

当前长期目标可以分为三层：

### 1.1 Classic / Compatibility

继续保持第一次讨论冻结的目标：

- Stock Yuri's Revenge 1.001；
- 传统 RA2YR MOD；
- 原版 MIX / INI / MAP / SHP / VXL / HVA / TMP / PAL 等数据；
- 尽可能零转换运行；
- 后续逐步研究 Ares / Phobos 等扩展语义。

### 1.2 红色际霸混合规则集

在兼容基础上，把 RA2 人类阵营与类似 SC2 神族、虫族的机制放进同一个 Runtime，用来验证：

- 不同生产方式能否共存；
- 不同基础设施约束能否共存；
- 不同建筑方式能否共存；
- 多武器、多护盾、多移动能力、多状态效果等是否真正数据驱动；
- UI 是否能够同时容纳 RA2 的全局建造便利性与 SC2 的单位技能式操作。

### 1.3 通用 MOD / RTS 能力

最终希望二改 MOD 作者能够在不改引擎源码的情况下定义新的：

- 单位；
- 武器；
- 护盾；
- 资源；
- 生产方式；
- 建筑方式；
- 移动能力；
- 基础设施体系；
- 技能；
- 状态效果；
- 科技升级；
- 老兵等级；
- 阵营规则。

本文暂不冻结统一 Entity / Component / Definition 数据模型；该部分将在后续“统一模型”专题中单独研究。

---

# 第二部分：命令、技能与单位操作

## 2. Command / Order / Ability 分层

已经确认采用如下方向：

```text
Command Card / UI / 热键 / AI
            ↓
         Command
            ↓
          Order
            ↓
   Unit / Entity Controller
```

核心含义：

- 移动、攻击、停止、攻击移动、巡逻等基础操作可以在技能栏 / Command Card 中显示为按钮；
- 但底层不能把所有行为都硬塞进“技能”一种类型；
- `Order` 才是驱动单位执行行为的统一运行时命令；
- Ability 是能够生成 Order 或产生 Effect 的一种高层能力来源；
- 玩家 UI、热键、AI 等最终应尽量走相同的 Command / Order 接口。

RA2 人类单位将获得现代 Command Card，包括但不限于：

- 移动；
- 攻击；
- 停止；
- 攻击移动；
- 巡逻；
- 原地防守；
- 对可部署单位显示部署 / 变形等能力。

---

# 第三部分：生产、建造与 UI

## 3. RA2 风格全局生产 UI 扩展为两级生产者选择

原 RA2 侧栏大致是：

```text
建筑 / 防御建筑 / 步兵 / 载具
```

新的混合 UI 继续保留分类入口，但增加生产者选择层。

逻辑结构：

```text
一级：生产类别
建筑 / 防御 / 步兵 / 载具 / 空军 / ...
        ↓
二级：当前类别下可用的生产者
建造厂A / 建造厂B / 战争工厂A / 战争工厂B / Gateway A / ...
        ↓
产品列表
该生产者当前允许生产的项目
```

例如美国人类阵营有多个盟军建造厂：

```text
[建筑]
  ↓
[建造厂 A] [建造厂 B] [建造厂 C]
  ↓
发电厂 / 兵营 / 矿场 / 战争工厂 / ...
```

有多个战争工厂时：

```text
[载具]
  ↓
[战争工厂 A] [战争工厂 B] [战争工厂 C]
  ↓
灰熊坦克 / 矿车 / IFV / ...
```

目标是同时获得：

- RA2 的全局生产便利性；
- 类似 RA3 的多生产建筑选择；
- SC2 的每座生产建筑拥有独立产能。

## 4. 工人建造建筑与全局生产栏可以并存

对于类似神族、虫族的阵营：

- 建筑 / 防御建筑仍由探机、工蜂等工人通过 Command Card 中的建造能力执行；
- 不要求把探机、工蜂强行放进全局侧栏作为“建筑生产者”；
- Gateway、机械台、星门等用于训练部队的建筑可以进入全局二级生产者 UI。

因此“是否显示在全局生产侧栏”应是生产者的可配置行为，而不是根据种族名称硬编码。

## 5. 每座生产建筑拥有独立产能

已经确认放弃 RA2 原版“同类型工厂共享单一生产进度、额外工厂主要提供加速”的核心生产语义，混合规则集采用：

> **每个生产者独立运行自己的生产能力。**

但引擎不能假设所有生产者只有一种普通队列。长期应允许不同 Production Policy / Mode，例如：

- 普通生产队列；
- 多并行生产线；
- 冷却式生产；
- 幼虫池式生产；
- Morph / Transform；
- 工人现场施工；
- RA2 侧栏式建造。

具体统一接口将在后续统一模型中冻结。

## 6. 多选生产建筑时的订单分配

默认行为：

- 只选择某一生产建筑时，生产订单只进入该建筑；
- `Ctrl + 点击` 等方式可多选 / 全选同类型生产建筑；
- 多个生产建筑被选中时，Command Card 类似 SC2 显示对应生产命令；
- **一次点击只增加一个生产订单，不是让所有选中建筑各生产一个；**
- 新增订单应在当前选中的生产者之间分配，使负载尽量均衡；
- 最终调度规则必须是确定性的（deterministic），避免 Replay / Multiplayer 因同帧排序不同产生分歧。

具体是严格轮询还是按预计完成时间分配，后续实现时可继续校准；当前冻结的是“一次点击一个订单 + 在选中生产者之间分配”的交互语义。

## 7. 可建造列表与前置条件共同决定生产资格

生产者本身定义“它能生产哪些项目”，同时每个产品仍然可以拥有自己的前置条件。

概念上：

```text
Producer Products
        ∩
Product Requirements
        ↓
当前真正可生产项目
```

因此不能只依靠 RA2 原版 `Prerequisite=`，也不能只依靠“建筑列表里写了什么”；两者共同作用。

## 8. 付款方式和建筑方式是规则特色，但底层应统一抽象

已确认保留不同阵营 / 规则集的差异：

- RA2 可保持渐进扣款；
- 类似 SC2 的生产可使用下单时一次性扣款；
- RA2 建筑可以侧栏完成后放置；
- 探机可以采用折跃式建筑；
- 工蜂可以采用工人变形式建筑；
- 其他阵营可以继续拥有新的建造方式。

这里的原则是：

> **玩法行为不强行统一，但引擎应提供统一的 Payment Policy / Construction Method 一类抽象，不应维护彼此完全独立的“RA2 付款系统”“SC2 付款系统”。**

---

# 第四部分：武器、护甲、生命和护盾

## 9. 武器系统暂时保留 RA2 兼容能力

当前不删除 RA2 传统武器体系的兼容能力。

原因：

- Weapon / Warhead / Projectile 不只是数值表达，还承载大量传统 MOD 语义；
- Classic / Compatibility 仍然需要读取传统 INI；
- 通用引擎未来可以在运行时进一步抽象，但不能为了新规则集直接破坏旧内容。

同时，新规则允许：

> **一个单位拥有多个武器。**

具体在什么时候选择哪一件武器，由单位数据 / Rules.ini 中的规则定义，而不是固定成传统 `Primary / Secondary` 二选一。

未来 Weapon Selector 可依据目标、距离、状态、优先级等条件选择武器；具体词典字段后续在统一 Rules.ini 研究中冻结。

## 10. 护甲暂采用 SC2 风格固定减伤

当前决定先采用简单的固定减伤逻辑，而不是百分比护甲：

```text
受到的基础伤害 - 对应护甲值 = 护甲处理后的伤害
```

例如：

```text
伤害 = 10
护甲 = 3
→ 护甲处理后伤害 = 7
```

最小伤害、穿甲、无视护甲、不同伤害层的精确顺序仍可在后续 Damage Pipeline 专题中冻结；本文不擅自新增最终规则。

## 11. 生命与护盾不共享护甲

已经明确：

- 生命值有自己的护甲；
- 护盾有自己的护甲；
- 护盾可以有自己的护甲类型 / 标签；
- 生命和护盾不能简单共用同一个 Armor 字段。

并且：

> **引擎必须允许一个实体拥有不止一种护盾。**

因此长期模型不能只写死：

```text
HP + Shield
```

而应允许多个按顺序处理的生命 / 防护层，例如：

```text
护盾 A
  ↓
护盾 B
  ↓
生命值
```

每一层可以拥有自己的：

- 当前值 / 最大值；
- 护甲；
- 护甲类型 / 标签；
- 恢复规则；
- 可被哪些效果影响。

**待讨论：** 多层护盾之间的溢出伤害如何处理，尚未冻结。

---

# 第五部分：基础设施、电力与种族特色

## 12. Power 与种族专属依赖可以同时存在

已经确认以下目标：

### 人类 / RA2 阵营

主要受全局电力体系影响，例如：

```text
发电厂 → 提供 Power
战争工厂 / 防御建筑 / 其他设施 → 消耗或依赖 Power
```

### 神族

水晶塔具有至少两种不同意义：

1. 可以作为通用 Power 的提供者，因此在规则允许时可以替代盟军火力发电厂为其他阵营设施供电；
2. 同时产生神族自己的局部能量场 / Psionic Field，某些神族建筑仍然必须位于该范围内。

因此：

> 有盟军发电厂不等于自动满足神族建筑的水晶塔范围要求。

### 虫族

虫族产能体系可以继续受自己的补给体系约束，例如王虫提供的补给。

即使虫族玩家通过俘获、结盟、混编等方式拥有：

- 人类发电厂；
- 神族水晶塔；

虫族自己的产能建筑仍然可以按照 Rules.ini 配置要求 `ZergSupply`，而不是自动改吃通用 Power。

## 13. 基础设施应按“需求通道”配置，而不是按阵营名称判断

理想数据语义类似：

```text
盟军战争工厂：需要 Power
Gateway：需要 Power，并且需要 Psionic Field
Hatchery：需要 ZergSupply
```

提供者则类似：

```text
人类发电厂：提供 Power
Pylon：提供 Power + Psionic Field
Overlord：提供 ZergSupply
```

这样才能自然支持跨阵营资源共享、占领建筑和二改 MOD，而不是出现：

```text
if (Faction == Zerg) ...
```

这类不可扩展的硬编码。

## 14. 缺少基础设施时的惩罚可以是阵营 / 单位特色

当前混合规则的总体想法是取消 SC2 的人口硬上限，把“缺少供给”转换成生产效率惩罚：

- 人类缺电时生产 / 建筑效率下降；
- 神族缺少相应基础设施时可以有自己的降速 / 停止规则；
- 虫族缺少补给时产能建筑可以变慢，而不是简单禁止继续生产。

具体倍率和不同建筑的惩罚方式属于 Rules.ini 平衡参数，不在本文冻结数值。

---

# 第六部分：资源与采集

## 15. 红色际霸规则集统一使用“资金”，但引擎保留多资源能力

当前规则集的可支付资源统一为：

```text
Credits / 资金
```

但引擎层不能把“只有一种资源”写死，因为二改 MOD 作者未来可能需要：

- 气矿；
- 石油；
- 稀有金属；
- 魔力；
- 其他自定义资源。

因此应区分：

### 可支付资源 / Currency

例如当前规则集只有：

```text
Credits
```

### 地图采集物 / Harvest Material

地图上可以同时存在：

- RA2 金矿；
- RA2 宝石矿；
- SC2 风格晶体矿；
- 黄金 / 富矿；
- 矿柱产生的矿；
- 未来其他采集节点。

不同采集物可以最终按自己的转换率进入同一种 `Credits`。

## 16. 采矿能力与采矿方式分离

当前设计允许：

- RA2 采矿车采 RA2 矿石；
- RA2 采矿车也可以采晶体矿簇；
- 神族探机 / 虫族工蜂可以采晶体矿；
- 神族探机 / 虫族工蜂也可以采矿柱产生的矿。

阵营本身不应该成为“能不能采某矿”的唯一判断条件。

同时，不同单位可以保留不同采集行为：

- 采矿车可以大量装载后回矿场卸货；
- 工人可以少量采集后频繁往返基地；
- 新 MOD 还可以定义新的采集 / 运送方式。

---

# 第七部分：科技与老兵系统

## 17. 人类阵营增加独立攻防科技升级

RA2 人类阵营后续可以增加类似 SC2 的攻防升级科技建筑，并设计自己的：

- 步兵攻击升级；
- 步兵防御升级；
- 载具攻击升级；
- 载具装甲升级；
- 空军升级；
- 国家 / 阵营特色升级。

底层仍应是通用 Upgrade 系统，不应分成 `RA2Upgrade` 和 `SC2Upgrade` 两套互不兼容的运行时。

## 18. 所有阵营都可以使用经验值老兵系统

不再沿用 RA2 原版“击杀自身价值若干倍目标后升级”的核心算法。

新的规则使用显式经验值：

- 每个单位在 Rules.ini 中规定自己死亡 / 被击杀后提供多少经验；
- 每个单位在 Rules.ini 中规定各等级需要多少经验；
- 廉价单位和贵重单位的经验收益由设计者独立平衡；
- 所有阵营原则上都可以拥有老兵 / 等级成长，不把它限制为 RA2 单位专属机制。

概念示例：

```ini
[SomeUnit]
ExperienceValue=...
VeteranExperience=...
EliteExperience=...
```

字段名只是示意，最终 Rules.ini 词典另行研究。

## 19. 击杀 / 经验归属可由 INI 自定义

对于：

- 附属炮塔；
- 航母舰载机；
- 召唤物；
- 临时单位；
- 其他子实体；

击杀归属、经验归属不强制使用唯一规则。

不同单位可以在 INI 中定义：

- 击杀记给自己；
- 击杀记给宿主；
- 经验给自己；
- 经验给宿主；
- 未来需要时定义其他归属策略。

具体字段与继承规则留到统一数据模型专题。

---

# 第八部分：隐身、潜地、伪装与幻象

## 20. 隐身与潜地属于“反隐对策”体系

当前规则：

- 隐身单位使用类似 SC2 的隐身机制；
- 潜地单位在侦测层面与隐身类似；
- 普通单位无法正常发现 / 锁定这类目标；
- 反隐 / Detector 单位能够发现隐身和潜地目标；
- 隐身单位与反隐单位形成明确的对策关系。

## 21. 伪装和幻象不等同于隐身

当前规则：

- 伪装不能被普通反隐单位自动识破；
- 幻象也不能简单等同于隐身目标；
- 初步设计允许玩家通过肉眼和行为判断发现异常；
- 后续可以加入专门的反伪装 / 情报识别单位或能力。

**待讨论：** 玩家肉眼识别出伪装敌人后，如何进行强制攻击、目标关系判断以及 UI 呈现，尚未冻结。

---

# 第九部分：高度、视野、射程与移动

## 22. 世界是具有高度属性的巨大格子地图

虽然最终画面主要保持 2D / 2.5D：

- 资源可以是 2D 图；
- 也可以使用 3D 模型预渲染成 2D；
- RA2 风格等距画面继续保留；

但 Simulation 中的地图格子具有真实高度属性。

高度效果只看**相对高度**，绝不使用地图绝对高度直接给固定加成。

### 攻击高度关系

比较：

```text
攻击者所在高度
    vs
目标所在高度
```

初步规则：攻击者相对目标每高两个高度格，可获得约一格攻击范围加成，最多加二格。

### 视野高度关系

比较：

```text
探索者所在高度
    vs
被探索地形 / 目标区域高度
```

高处可以获得额外视野；最终还需结合遮挡等机制研究。

### 飞行单位

**飞行单位不吃地形高度带来的视野 / 射程地形加成。**

飞行高度属于自身运动 / 空间语义，不因为脚下山地更高就再次获得地形奖励。

## 23. 移动能力采用组合式表达

已确认需要支持包括但不限于：

```text
Ground
Water
Air
CliffTraverse
Burrow / Tunnel
Jump / Jetpack
```

示例：

```text
犀牛坦克 → Ground + 履带运动特性
海豚 → Water
两栖运输艇 → Ground + Water
凤凰 → Air
巨像 → Ground + CliffTraverse
潜地单位 → Ground + Burrow/Tunnel（按具体规则）
喷气背包单位 → Ground + Jump/Jetpack
```

同时需要区分：

- “能去哪里”的 Movement Capability；
- “怎么移动”的 Locomotion，例如履带、轮式、步行、悬浮等。

最终字段在统一 Rules.ini 词典中研究。

---

# 第十部分：炮塔、多武器、状态与变形

## 24. 武器选择规则属于单位数据

单位可以拥有多个武器，并在自己的 Rules.ini 定义：

- 某个武器对什么目标使用；
- 何时优先使用；
- 不同条件下切换哪一个武器。

引擎不能把武器选择永久写死为传统 RA2 的 `Primary / Secondary`。

## 25. 炮塔可以作为宿主上的特殊附属实体

允许一个单位拥有一个或多个炮塔。

炮塔可以拥有自己的：

- 武器；
- 朝向 / 旋转；
- 目标选择；
- 状态；
- 可选独立生命值和护甲。

是否拥有独立血量是可配置项：

```text
IndependentHealth = yes / no
```

概念字段名仅为示意。

炮塔的瞄准方式也可配置，例如：

- 自行寻找和攻击目标；
- 跟随宿主当前攻击目标；
- 混合模式。

为了避免大型单位拥有大量炮塔时把每个炮塔都当作完整可寻路 Unit 带来不必要开销，长期统一模型更适合把它表达为 Attached Entity / Hardpoint 一类附属实体；最终结构待统一模型专题冻结。

## 26. 状态效果系统作为一级 Gameplay 系统

已经确认需要通用 Status Effect / Buff 系统，而不是为每种特殊单位写专用逻辑。

未来应能够表达诸如：

- 减速；
- 晕眩；
- 定身；
- 沉默；
- 武器禁用；
- 隐身；
- 无敌；
- 护甲增减；
- 伤害增减；
- 速度增减；
- 持续伤害；
- 持续治疗；
- 心灵控制；
- 揭示 / 反隐；
- EMP；
- 辐射；
- 其他持续或瞬时状态。

## 27. 部署、变形、Morph 统一进入通用变形能力

RA2 和 SC2 中大量“部署 / 变形”行为应共享同一类底层能力，而不是每个单位单独写逻辑。

例如：

```text
MCV ↔ 建造厂
GI 部署
武装直升机部署
坦克攻城模式
Viking 模式切换
Gateway ↔ Warp Gate
虫族 Morph
Burrow / Unburrow（如最终按变形规则表达）
```

变形过程中哪些状态继承（生命、护盾、经验、Buff、Cargo、冷却等）仍需在统一模型中明确，不在本文提前冻结。

---

# 第十一部分：航空与战略能力

## 28. 航空单位允许完全不同的后勤方式

飞机 / 飞行单位不使用唯一规则。

允许同时存在：

### RA2 风格航空单位

例如黑鹰：

- 需要机场；
- 有弹药限制；
- 攻击后返航；
- 在机场补弹。

### SC2 风格自由飞行单位

例如凤凰：

- 长期自由飞行；
- 不要求机场；
- 不要求采用 RA2 弹药返航机制。

这被视为阵营科技和玩法差异。

底层应通过可选的 Air Movement / Ammo / Home Base 等能力组合表达，而不是“飞机都必须返航”或“飞机都永久飞行”。

## 29. 左侧建立独立超级武器 / 战略能力栏

超级武器不全部塞进右下角 Command Card。

计划在左侧提供独立的战略能力栏，用于：

- 核弹；
- 天气控制；
- 铁幕；
- 超时空类能力；
- 轨道扫描；
- 阵营全局技能；
- 未来其他战略能力。

能力来源可以是建筑、科技、阵营、地图等；最终 Provider 模型后续研究。

---

# 第十二部分：选择与 Command Card

## 30. 多选单位采用 SC2 式 subgroup

已经确认：

```text
选择集合
  ↓
按 UnitType / 类型分组
  ↓
当前激活 subgroup
  ↓
显示该 subgroup 的 Command Card
```

玩家可以在多个单位类型之间切换 subgroup，而不是把所有不同单位的所有按钮混在同一个技能栏中。

---

# 第十三部分：开局规则

## 31. 不同阵营允许不同开局载体

当前概念：

### RA2 人类阵营

```text
MCV 开局
```

可以没有其他初始部队。

### 神族 / 虫族

可以：

```text
主基地 + 1 个初始工人
```

以避免“完全没有建造者”造成无意义的开局空档。

具体初始资源、工人数、首农免费与否属于后续平衡问题，目前不冻结。

---

# 第十四部分：目标关系（概念说明，尚未作为最终规则冻结）

## 32. “Own / Ally / Neutral / Enemy”到底是什么意思

这里不是在讨论英语命名，而是在讨论：

> **一件武器、技能、治疗、占领、心灵控制到底允许对哪些关系的目标生效。**

中文可以理解为：

- `Own`：自己控制的单位；
- `Ally`：盟友单位；
- `Neutral`：中立单位 / 建筑；
- `Enemy`：敌对单位；
- 未来还可能有中立敌对生物、临时控制单位等。

例如：

```text
治疗：允许对自己 + 盟友
维修：允许对自己 + 盟友中的机械单位
心灵控制：允许对敌人
工程师占领：可能允许对敌方 / 中立建筑
```

伪装机制又会带来一个重要区别：

```text
真实所属阵营
    ≠
敌人当前看到的伪装身份
```

因此底层未来很可能需要区分“真实关系”和“感知身份”。

**状态：** 这一节只是把概念翻译清楚，具体关系系统、伪装后的强制攻击规则和字段设计尚未冻结，留到统一模型 / 伪装专题继续讨论。

---

# 第十五部分：AI 与 Trigger

## 33. AI 将成为重点专题

长期目标不是复刻 RA2 `aimd.ini` 那种主要依赖固定小队和资源作弊的 AI。

计划研究：

- 更拟人的操作逻辑；
- 神经网络 / 机器学习；
- Replay 数据；
- 模仿学习；
- Self-play；
- 训练与评估；
- 不依赖给 AI 无限制加钱来制造难度。

但 AI 体系尚未冻结。

已经确定的最低架构原则是：

> **未来 AI 应尽量通过与真人玩家相同的 Command / Order 接口控制单位，而不是拥有一套完全绕开正常规则的“AI 专用控制器”。**

后续需要单独讨论：

- Observation / AI 能看到什么；
- Fog of War 下是否允许 AI 读取不可见信息；
- Action Space；
- Headless Simulation；
- Determinism；
- Replay；
- 训练环境；
- 难度分级；
- 战略 AI 与微操 AI 是否分层；
- 传统脚本 AI 是否仍作为基线和测试对手保留。

## 34. Trigger 将成为重点专题

RA2 战役、任务地图和大量 MOD 高度依赖 Trigger。

当前不冻结新的 Trigger DSL / 脚本语言，但已经确认：

- Trigger 不能被忽略；
- Classic RA2 地图兼容最终仍然需要理解原版 Event / Action 等语义；
- 通用 RTS 引擎未来还需要表达新 Ability、Upgrade、Production、Status 等事件。

后续专题需要研究：

- 原版 RA2 Trigger 数据模型；
- Event / Condition / Action；
- 新旧 Trigger 如何共存；
- 是否增加脚本层；
- 是否增加可视化编辑器；
- Replay / Multiplayer 中 Trigger 的确定性执行；
- Trigger 与统一 Game Event 系统的边界。

---

# 第十六部分：当前冻结的架构红线

截至本次讨论，可以把以下原则作为后续代码审查的阶段性红线：

1. Command Card / UI 负责产生 Command，Order 驱动实体实际行为；Ability 不等于所有 Order。
2. 不同阵营的玩法行为可以不同，但底层尽量共享抽象接口和数据模型。
3. 避免用 `if (Faction == ...)` 大量硬编码阵营特色；优先使用组件、能力、策略、Tag、Requirement、数据配置表达。
4. 生产者与产品是多对多关系，产品自己的前置条件继续参与资格判定。
5. 每座生产建筑拥有独立产能，但生产模式本身必须可扩展，不写死成一种普通 Queue。
6. RA2 全局生产便利性与 SC2 独立生产建筑可以同时存在。
7. 地图采集物与玩家可支付资源是两个概念；当前红色际霸只花 Credits，不代表引擎永远只有一种货币。
8. 生命与护盾拥有独立护甲；实体允许拥有多层护盾 / 防护层。
9. 单位允许任意数量的武器；武器选择条件由数据定义。
10. 移动能力采用组合式 Capability，移动介质与履带 / 轮式 / 步行等 Locomotion 概念分开。
11. 地形高度加成只计算相对高度；飞行单位不吃地形高度奖励。
12. 炮塔允许作为宿主附属实体存在，并可配置独立血量与独立 / 跟随宿主瞄准。
13. 状态效果是一级通用 Gameplay 系统。
14. 部署 / Morph / Transform 进入通用变形能力，不为每个单位复制特殊流程。
15. 航空后勤是可选能力，不把“返航补弹”或“永久飞行”写死成所有飞机的规则。
16. 超级武器 / 战略能力拥有独立全局 UI 入口。
17. 所有阵营原则上都能使用经验值老兵系统；击杀 / 经验归属允许按单位数据配置。
18. AI 最终应尽量走正常玩家 Command / Order 接口。
19. Classic / Compatibility 不因通用化而降级；传统 RA2YR 数据兼容仍然是第一类需求。

---

# 第十七部分：下一轮专项研究

本次讨论到此不继续扩张范围。

下一阶段优先单独研究三个主题：

## A. AI

重点：

- 拟人操作目标；
- 传统规则 AI、行为 AI、神经网络 AI 的关系；
- Observation / Action；
- Fog of War；
- Replay；
- Headless Simulation；
- Self-play / 模仿学习；
- Determinism 与 Multiplayer。

## B. Trigger

重点：

- RA2 原版 Event / Action / Trigger；
- 战役和 MOD 兼容；
- 通用 Game Event；
- 新机制如何进入 Trigger；
- 脚本 / 可视化编辑的可能性；
- 确定性执行。

## C. 统一模型与 Rules.ini 属性词典

用户计划整理 / 研究一套更系统的单位属性 `Rules.ini` 词典。

届时重点研究：

```text
Definition 数据层
        ↓
继承 / Override / Patch
        ↓
Validation
        ↓
Runtime Entity / Components
```

并逐步冻结：

- 单位 Definition；
- Tag；
- Health / Shield / Damage Layer；
- Weapon；
- Ability；
- Production；
- Requirement；
- Movement；
- Infrastructure；
- Veterancy；
- Attached Entity / Turret；
- Status Effect；
- Transform；
- Resource；
- AI metadata；
- Trigger / Game Event 连接点；
- RA2 原版字段如何映射到新的通用模型。

在该专题完成前，不应为了赶进度随意冻结大量新的 INI 字段名。

---

# 结论

第二次讨论把项目目标从：

```text
C++ 重写 RA2YR 兼容 Runtime
```

进一步扩展为：

```text
RA2YR Classic / Compatibility
          +
红色际霸混合规则压力测试
          +
数据驱动通用 RTS Runtime
```

但三者不是互相替代关系。

最关键的工程约束仍然是：

> **新机制必须证明自己可以在不破坏 Classic 兼容的情况下进入统一 Runtime。玩法可以不同，抽象应尽量统一；阵营特色应该来自数据，而不是不断增长的阵营特判。**

下一份正式讨论记录应优先围绕 AI、Trigger 或统一 Rules.ini / Entity 模型中的一个专题展开，而不是继续无边界增加零散玩法条目。
